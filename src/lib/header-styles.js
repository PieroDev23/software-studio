const headerBrandClass =
  "inline-flex shrink-0 items-center whitespace-nowrap font-mono text-xs font-medium uppercase leading-none tracking-[0.18em] sm:text-sm";

const headerNavClass = "hidden items-center gap-8 md:flex";

const headerNavItemClass =
  "font-mono text-xs font-medium uppercase leading-none tracking-[0.14em]";

const headerCtaClass =
  "inline-flex items-center justify-center bg-white px-4 py-2.5 font-mono text-xs font-semibold uppercase leading-none tracking-[0.12em] text-black transition-colors hover:bg-accent hover:text-white";

export { headerBrandClass, headerCtaClass, headerNavClass, headerNavItemClass };
